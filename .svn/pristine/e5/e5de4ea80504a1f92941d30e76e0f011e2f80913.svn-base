import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DirectoryStateComponent } from './directory-state.component';

describe('DirectoryStateComponent', () => {
  let component: DirectoryStateComponent;
  let fixture: ComponentFixture<DirectoryStateComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DirectoryStateComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DirectoryStateComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
