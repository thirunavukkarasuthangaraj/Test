import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DirectoryHeaderComponent } from './directory-header.component';

describe('DirectoryHeaderComponent', () => {
  let component: DirectoryHeaderComponent;
  let fixture: ComponentFixture<DirectoryHeaderComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DirectoryHeaderComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DirectoryHeaderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
