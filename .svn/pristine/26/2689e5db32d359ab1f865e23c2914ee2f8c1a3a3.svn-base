import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BusinesDirectoryHealthcareLandingComponent } from './busines-directory-healthcare-landing.component';

describe('BusinesDirectoryHealthcareLandingComponent', () => {
  let component: BusinesDirectoryHealthcareLandingComponent;
  let fixture: ComponentFixture<BusinesDirectoryHealthcareLandingComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BusinesDirectoryHealthcareLandingComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BusinesDirectoryHealthcareLandingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
