import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BusinesDirectoryProfileComponent } from './busines-directory-profile.component';

describe('BusinesDirectoryProfileComponent', () => {
  let component: BusinesDirectoryProfileComponent;
  let fixture: ComponentFixture<BusinesDirectoryProfileComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BusinesDirectoryProfileComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BusinesDirectoryProfileComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
