import { ComponentFixture, TestBed } from '@angular/core/testing';

import { LayoutJobsComponent } from './layout-jobs.component';

describe('LayoutJobsComponent', () => {
  let component: LayoutJobsComponent;
  let fixture: ComponentFixture<LayoutJobsComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ LayoutJobsComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(LayoutJobsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
