// import { Action, UPDATE, createAction, props } from "@ngrx/store";

// // export const increment = createAction('[Counter] Increment');
// // export const decrement = createAction('[Counter] Decrement');
// // export const setPlan = createAction('[Plan] Set', props<{ plan: string }>)

// export const UPDATE_NAME = '';

// export class UpdatePlan implements Action {
//     readonly type = UPDATE;
//     constructor(public planName: string){}
// }

// export type Actions = UpdatePlan;