import { MessageuniquePipe } from './messageunique.pipe';

describe('MessageuniquePipe', () => {
  it('create an instance', () => {
    const pipe = new MessageuniquePipe();
    expect(pipe).toBeTruthy();
  });
});
